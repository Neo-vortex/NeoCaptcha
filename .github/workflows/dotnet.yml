name: .NET

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore --configuration Release

    - name: Test
      run: dotnet test --no-build --verbosity normal --configuration Release

    - name: Pack NeoCaptcha
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: dotnet pack src/NeoCaptcha/NeoCaptcha.csproj --no-build --configuration Release --output ./nupkgs

    - name: Pack NeoCaptcha.AspnetCore
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: dotnet pack src/NeoCaptcha.AspnetCore/NeoCaptcha.AspnetCore.csproj --no-build --configuration Release --output ./nupkgs

    - name: Push to NuGet
      if: github.event_name == 'push' && github.ref == 'refs/heads/main'
      run: dotnet nuget push "./nupkgs/*.nupkg" --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate
